import Link from 'next/link';
import {ArrowRight,Check} from 'lucide-react';
import {plannerOptions} from '@/lib/data';

export default function Packages(){return <main className="catalogue"><section className="pageHero"><Link href="/" className="backLink">← Back home</Link><div><p className="eyebrow">PACKAGES · STARTING POINTS</p><h1>A little structure.<br/><em>Infinite room.</em></h1><p>Our packages make the first conversation easier. Every final proposal is tailored to the event.</p></div></section><section><div className="sectionTop"><div><p className="eyebrow">THREE SCALES</p><h2 className="headline">Choose your<br/><em>starting point.</em></h2></div></div><div className="testimonialGrid">{plannerOptions.map(p=><article className="packageCard" key={p.value}><p className="eyebrow">{p.label}</p><h3>{p.price.toLocaleString('en-IN')}</h3><small>{p.detail}</small><div className="includes">{p.includes.map(x=><div key={x}><Check size={14}/>{x}</div>)}</div><Link className="sectionLink" href="/planner">Choose {p.label} <ArrowRight size={14}/></Link></article>)}</div></section><section className="catalogueFooter"><h2>Need something<br/><em>more bespoke?</em></h2><Link href="/planner">Build a custom brief <ArrowRight size={16}/></Link></section></main>}
