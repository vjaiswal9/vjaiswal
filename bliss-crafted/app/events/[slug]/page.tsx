import Link from 'next/link';
import {notFound} from 'next/navigation';
import {ArrowRight,ArrowUpRight} from 'lucide-react';
import {events} from '@/lib/data';

export function generateStaticParams(){return events.map(event=>({slug:event.slug}))}
export default async function EventDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const event=events.find(x=>x.slug===slug);if(!event)return notFound();return <main className="eventDetail"><section className="detailHero" style={{backgroundImage:`linear-gradient(90deg,rgba(8,8,7,.7),rgba(8,8,7,.15)),url(${event.image})`}}><Link href="/events" className="detailBack">← All experiences</Link><div className="detailHeroCopy"><p className="eyebrow">{event.eyebrow}</p><h1>{event.title}</h1><p>{event.accent}</p></div></section><section className="detailIntro"><div><p className="eyebrow">THE EXPERIENCE</p><h2>Made around<br/><em>your moment.</em></h2></div><div><p>{event.description}</p><p>Our dummy catalogue is intentionally flexible. The next milestone will connect this experience to real packages, guest counts, add-ons, availability and quotation generation.</p><Link href="/#planner" className="detailCta">Start planning <ArrowUpRight size={16}/></Link></div></section><section className="detailServices"><p className="eyebrow">WHAT WE CAN CRAFT</p><div className="detailList">{['Creative direction','Décor & spatial design','Production & coordination','Hospitality & guest experience','Entertainment & technical production','Photography & content'].map((x,i)=><div key={x}><span>0{i+1}</span><h3>{x}</h3><ArrowRight size={18}/></div>)}</div></section><section className="detailEnd"><p className="eyebrow">READY WHEN YOU ARE</p><h2>Let's make it<br/><em>unforgettable.</em></h2><Link href="/#contact">Start a conversation <ArrowRight size={16}/></Link></section></main>}