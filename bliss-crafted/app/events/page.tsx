import Link from 'next/link';
import {ArrowUpRight,ArrowRight} from 'lucide-react';
import {events} from '@/lib/data';

export default function EventsPage(){return <main className="catalogue"><header className="catalogueHero"><Link className="backLink" href="/">← Bliss Crafted</Link><div><p className="eyebrow">THE BLISS CRAFTED CATALOGUE</p><h1>Choose your<br/><em>kind of magic.</em></h1><p>Explore the worlds we create — then tell us which one feels most like you. Every experience can be shaped around your people, place and priorities.</p></div></header><section className="eventGrid">{events.map((event,i)=><Link className="eventTile" href={`/events/${event.slug}`} key={event.slug}><div className="eventImage" style={{backgroundImage:`url(${event.image})`}}/><div className="eventShade"/><div className="eventInfo"><small>{event.eyebrow}</small><h2>{event.title}</h2><p>{event.description}</p><div className="tagRow">{event.tags.map(tag=><span className="tag" key={tag}>{tag}</span>)}</div><span>Explore experience <ArrowRight size={14}/></span></div><b className="eventIndex">0{i+1}</b></Link>)}</section><section className="catalogueFooter"><p className="eyebrow">YOUR EVENT, YOUR RULES</p><h2>Don't see exactly<br/>what you <em>imagined?</em></h2><Link href="/planner">Build something bespoke <ArrowUpRight size={16}/></Link></section></main>}
