'use client';
import {useState} from 'react';
import Link from 'next/link';
import {ArrowRight} from 'lucide-react';

export default function Login(){const [mode,setMode]=useState<'login'|'signup'>('login');const [sent,setSent]=useState(false);return <main className="authPage"><div className="authVisual"/><div className="authPanel"><Link href="/" className="backLink">← Bliss Crafted</Link>{sent?<><p className="eyebrow">DEMO ACCOUNT</p><h1>Welcome<br/><em>inside.</em></h1><p>This prototype routes you to the client dashboard. Real authentication can be connected later.</p><Link className="darkButton" href="/dashboard">Open dashboard <ArrowRight size={16}/></Link></>:<><p className="eyebrow">CLIENT PORTAL</p><h1>{mode==='login'?'Welcome back.':'Begin here.'}</h1><p>{mode==='login'?'Access your event workspace, proposal and timeline.':'Create a client workspace for your event.'}</p><div className="formCard"><div className="formGrid"><div className="field full"><label>Email</label><input type="email" placeholder="you@example.com"/></div><div className="field full"><label>Password</label><input type="password" placeholder="••••••••"/></div></div><div className="formActions"><button className="darkButton" onClick={()=>setSent(true)}>{mode==='login'?'Sign in':'Create account'} <ArrowRight size={16}/></button></div></div><button className="authSwitch" onClick={()=>setMode(mode==='login'?'signup':'login')}>{mode==='login'?'New here? Create an account':'Already have an account? Sign in'}</button></>}</div></main>}
